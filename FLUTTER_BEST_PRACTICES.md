# Flutter Best Practices - Правила лучших практик (Новая версия)

## 📋 Оглавление
1. [Основные принципы разработки](#основные-принципы-разработки)
2. [Архитектура и структура проекта](#архитектура-и-структура-проекта)
3. [Кодирование и стиль](#кодирование-и-стиль)
4. [Тестирование и поддержка](#тестирование-и-поддержка)
5. [Чеклист и итоги](#чеклист-и-итоги)

---

## 🎯 Основные принципы разработки

### 1. Принципы SOLID и DRY

**SOLID:**
- **Single Responsibility** - каждый класс отвечает за одну задачу
- **Open/Closed** - открыт для расширения, закрыт для модификации
- **Liskov Substitution** - реализации могут заменять интерфейсы
- **Interface Segregation** - много маленьких интерфейсов лучше одного большого
- **Dependency Inversion** - зависимость от абстракций, а не от конкретных классов

**DRY (Don't Repeat Yourself):**
- Выносите повторяющийся код в переиспользуемые виджеты
- Используйте константы вместо магических чисел
- Создавайте утилитные классы для общей логики

### 2. Именование интерфейсов

❌ **НЕПРАВИЛЬНО:**
```dart
class IEmailValidator {
  String? validate(String? email);
}

class EmailValidatorImpl implements IEmailValidator { ... }
```

✅ **ПРАВИЛЬНО:**
```dart
abstract class EmailValidator {
  ValidationError? validate(String? email);
}

class DefaultEmailValidator implements EmailValidator { ... }
```

**Правило:** Не используйте префикс `I` для интерфейсов. Имя должно отражать роль, а не тип. Реализации должны иметь описательные имена.

### 3. Разделение бизнес-логики и UI

❌ **НЕПРАВИЛЬНО:**
```dart
class LoginViewModel {
  Future<String> login(String email, String password) async {
    return 'Вход выполнен успешно!'; // ❌ Строка из бизнес-логики
  }
}
```

✅ **ПРАВИЛЬНО:**
```dart
class LoginViewModel {
  Future<LoginResult> login(String email, String password) async {
    return LoginResult.success(); // ✅ Типизированный результат
  }
}

class ErrorMessageService {
  static String? getMessage(LoginResult result) {
    switch (result.status) {
      case LoginStatus.success:
        return 'Вход выполнен успешно!'; // ✅ UI слой преобразует
    }
  }
}
```

**Правило:** Бизнес-логика возвращает типизированные объекты, а UI слой преобразует их в текст. Это упрощает добавление мультиязычности.

---

## 🏗️ Архитектура и структура проекта

### 1. Архитектура MVVM

**Стандартизированная структура проекта:**
```
lib/
├── main.dart              # Точка входа приложения
├── app.dart              # Корневой виджет приложения
├── pages/                # Экраны/страницы приложения
│   ├── home_page.dart
│   └── login_page.dart
├── widgets/              # Переиспользуемые виджеты
│   ├── app_button.dart
│   └── text_field.dart
├── models/               # Модели данных
│   └── user.dart
├── viewmodels/           # Бизнес-логика и состояние
│   └── auth_viewmodel.dart
├── services/             # Сервисы (API, валидаторы)
│   └── api_service.dart
├── themes/               # Темы приложения
│   └── app_theme.dart
└── constants/            # Константы приложения
    └── app_constants.dart
```

**Разделение ответственности:**
- **View (Pages/Widgets)** - только отображение UI, обработка пользовательского ввода
- **ViewModel** - бизнес-логика, состояние, преобразование данных для View
- **Model** - чистые данные, без логики
- **Service** - работа с внешними источниками (API, базы данных)

### 2. Правила именования папок и файлов

| Папка | Назначение | Пример файла |
|-------|------------|--------------|
| `pages/` | Экраны/страницы | `home_page.dart` |
| `widgets/` | Переиспользуемые виджеты | `app_button.dart` |
| `models/` | Модели данных | `user.dart` |
| `viewmodels/` | Бизнес-логика | `auth_viewmodel.dart` |
| `services/` | Сервисы | `api_service.dart` |
| `themes/` | Темы оформления | `app_theme.dart` |
| `constants/` | Константы | `app_constants.dart` |

### 3. Создание структуры папок

**Принцип YAGNI (You Aren't Gonna Need It):**
- Не создавайте пустые папки заранее "на будущее"
- Создавайте папки только когда в них появляются файлы
- Начинайте с минимальной структуры и расширяйте по мере роста проекта

**Минимальная структура для нового проекта:**
```
lib/
├── main.dart
├── app.dart
└── pages/
    └── home_page.dart
```

---

## 🔧 Кодирование и стиль

### 1. Избегание методов, возвращающих виджеты

❌ **НЕПРАВИЛЬНО:**
```dart
class MyWidget extends StatelessWidget {
  Widget _buildButton({required String text}) {
    return ElevatedButton(...);
  }
}
```

✅ **ПРАВИЛЬНО:**
```dart
class MyButton extends StatelessWidget {
  final String text;
  const MyButton({super.key, required this.text});
  
  @override
  Widget build(BuildContext context) {
    return ElevatedButton(...);
  }
}
```

**Правило:** Выносите части UI в отдельные классы-виджеты вместо методов.

### 2. Использование константных конструкторов

❌ **НЕПРАВИЛЬНО:**
```dart
class LoginCredentials {
  final String email;
  final String password;
  
  LoginCredentials({required this.email, required this.password});
}
```

✅ **ПРАВИЛЬНО:**
```dart
class LoginCredentials {
  final String email;
  final String password;
  
  const LoginCredentials({
    required this.email,
    required this.password,
  });
}
```

**Применяется к:**
- Моделям данных с `final` полями
- `StatelessWidget` виджетам
- ViewModels и Services (для улучшения тестируемости)

### 3. Разделение виджетов на отдельные файлы

❌ **НЕПРАВИЛЬНО:** Все виджеты в одном файле  
✅ **ПРАВИЛЬНО:** Каждый виджет в отдельном файле

**Правило:** Это улучшает читаемость кода, упрощает навигацию и поддерживает принцип единственной ответственности.

### 4. Избегание избыточных комментариев

❌ **НЕПРАВИЛЬНО:**
```dart
// Индекс текущего видимого контейнера (0 или 1)
int _currentIndex = 0;
```

✅ **ПРАВИЛЬНО:**
```dart
int _currentIndex = 0;
```

**Правило:** Код должен быть самодокументированным через понятные имена. Комментарии должны объяснять "почему", а не "что".

---

## 🧪 Тестирование и поддержка

### 1. Проверка тестов после изменений

**Правило:** После любых изменений в `lib/` проверяйте и обновляйте тесты:
- Перемещение классов между файлами
- Изменение структуры проекта  
- Переименование классов или файлов
- Изменение импортов

**Команда:** `flutter test`

### 2. Применение константных конструкторов во ViewModels и Services

❌ **НЕПРАВИЛЬНО:**
```dart
class ContactService {
  List<Contact> getContacts() { ... }
}

class ContactsViewModel {
  final ContactService contactService;
  ContactsViewModel(this.contactService); // ❌ Нет const конструктора
}
```

✅ **ПРАВИЛЬНО:**
```dart
class ContactService {
  const ContactService(); // ✅ Const конструктор
  List<Contact> getContacts() { ... }
}

class ContactsViewModel {
  final ContactService contactService;
  const ContactsViewModel({ContactService? contactService})
      : contactService = contactService ?? const ContactService(); // ✅ Const конструктор с DI
}
```

**Правило:** Все ViewModels и Services должны иметь const конструкторы для улучшения тестируемости.

---

## ✅ Чеклист и итоги

### Быстрый чеклист перед коммитом
- [ ] Соблюдена архитектура MVVM
- [ ] Применены принципы SOLID и DRY
- [ ] Бизнес-логика отделена от UI
- [ ] Используются константные конструкторы
- [ ] Нет методов, возвращающих виджеты
- [ ] Все тесты проходят успешно
- [ ] Удалены избыточные комментарии

### Преимущества стандартизированной структуры
1. **Единообразие** - все проекты выглядят одинаково
2. **Быстрая onboarding** - новые разработчики быстро понимают структуру
3. **Легкая навигация** - знаешь где что искать
4. **Масштабируемость** - структура растет вместе с проектом
5. **Улучшенная тестируемость** - легко mock-ить зависимости

### Миграция со старой структуры
1. Создайте необходимые папки
2. Переместите файлы в соответствующие папки
3. Обновите импорты
4. Добавьте const конструкторы
5. Проверьте и обновите тесты
6. Запустите приложение

**Помните:** Консистентность важнее перфекционизма. Начните с новой структуры в новых проектах, а существующие проекты мигрируйте постепенно по мере необходимости.

---

## 📚 Дополнительные ресурсы
- [Flutter Style Guide](https://dart.dev/guides/language/effective-dart/style)
- [Flutter Architecture Samples](https://github.com/brianegan/flutter_architecture_samples)
- [Clean Architecture for Flutter](https://blog.cleancoder.com/uncle-bob/2012/08/13/the-clean-architecture.html)

---

*Документ создан на основе оригинального файла FLUTTER_BEST_PRACTICES.md*
*Дата создания: 2024*