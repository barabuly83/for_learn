rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Разрешить чтение и запись для всех аутентифицированных пользователей
    // В тестовом режиме - разрешить все операции
    match /{allPaths=**} {
      allow read, write: if true;
    }
  }
}